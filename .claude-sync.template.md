# Claude Instances Sync File

This file coordinates work between multiple Claude Code instances.
**Check this file before starting any work. Update it when you start/finish tasks.**

## Rules

1. **Minimal ownership**: Only claim files you're actively editing. Release immediately when done.
2. **Check before editing**: Always check this file before modifying any file.
3. **Update status**: Keep your instance status current.

## Dev Server Ports

| Instance | Port | Command |
|----------|------|---------|
| A | 3000 | `npm run dev` (default) |
| B | 3001 | `npm run dev -- -p 3001` |

## Active Instances

### Instance A (worktree-a)
- **Status**: Idle
- **Working on**: None
- **Files being edited**: None
- **Dev server**: http://localhost:3000
- **Last update**:

### Instance B (worktree-b)
- **Status**: Idle
- **Working on**: None
- **Dev server**: http://localhost:3001
- **Files being edited**: None
- **Last update**:

## File Ownership (to avoid conflicts)

| Directory/File | Owner | Notes |
|---------------|-------|-------|
| `frontend/app/page.tsx` | Available | Main chat component |
| `frontend/lib/*` | Available | Utility modules |
| `frontend/components/*` | Available | UI components |
| `contracts/src/*` | Available | Smart contracts |
| `contracts/test/*` | Available | Contract tests |

## Completed Tasks Queue (ready to merge to main)

- [ ] Instance A: (not started)
- [ ] Instance B: (not started)

## Communication

To communicate with another instance, add a message here:

```
[From A to B]: <message>
[From B to A]: <message>
```

## Merge Instructions

When ready to merge:
1. Ensure all tests pass: `npm run test:run` / `forge test`
2. Update this file to mark task complete
3. From main repo: `git checkout main && git merge <branch-name>`
4. Push: `git push origin main`
